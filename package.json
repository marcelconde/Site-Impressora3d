{
  "name": "forgecon",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "dev:api": "wrangler dev --port 8787",
    "check:worker": "wrangler deploy --dry-run --outdir /tmp/forgecon-worker-build"
  },
  "dependencies": { "pdf-lib": "^1.17.1" },
  "devDependencies": { "wrangler": "^4.0.0", "@playwright/test": "^1.55.0" }
}
